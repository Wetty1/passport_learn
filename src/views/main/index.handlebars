<h1>Hello Express + passport</h1>
<div class="col-xs-12 col-md-12">
    {{#if user}}
    <a href="/auth/logout">logout</a>
    {{/if}}
</div>
<div class="col-xs-12 col-md-12">
    {{#if user}}
        <h2>You are logged</h2>
    {{else}}
        <h2>Please log in</h2>
    {{/if}}
</div>